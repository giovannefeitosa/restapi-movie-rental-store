const { check } = require('express-validator');
const { handleErrors } = require('../helpers');
const models = require('../models');

module.exports = function(app) {
  app.post('/rent_movie', (req, res) => {
    //
  });

  app.post('/return_movie', (req, res) => {
    //
  });
};
